name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs for the same ref (new push to a PR supersedes the old).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/verify.yml
